{
  "name": "netflix-clone-app",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:5000",
  "comment": "The proxy^ above points to the server's program",
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-html-parser": "^2.0.2",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "dev-start": "start npm run dev-client && start npm run dev-server",
    "dev-server": "cd server && npm run dev-start",
    "dev-client": "react-scripts start",
    "deploy-start": "tbd.."
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
